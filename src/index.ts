// @ts-ignore
import { StandardEngine } from 'standard-engine'

import { options } from './options'

// eslint-disable-next-line @typescript-eslint/no-unsafe-call,import/no-default-export
export default new StandardEngine(options)
